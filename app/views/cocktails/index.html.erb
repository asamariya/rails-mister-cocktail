<h1>Cocktails</h1>

<ul>
  <% @cocktails.each do |cocktail| %>
    <li>
      <%= cl_image_tag cocktail.photo, height: 150, width: 150, crop: :thumb %>
      <%= link_to cocktail.name, cocktail %>
    </li>
  <% end %>
</ul>

<%= link_to 'Add a new cocktail', new_cocktail_path  %>

